import 'package:flutter/material.dart';

class ConprehensiveList extends StatelessWidget {
  const ConprehensiveList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 180,
      height: 122,
    );
  }
}
